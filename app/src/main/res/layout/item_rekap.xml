<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="12dp"
    android:layout_marginBottom="8dp"
    android:background="@drawable/shape_rounded_item"
    android:gravity="center_vertical">

    <ImageView
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:src="@android:drawable/checkbox_on_background"
        app:tint="@color/sage_green"
        android:layout_marginEnd="16dp"/>

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tvNamaMhs"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Tanggal"
            android:textStyle="bold"
            android:textColor="@color/text_brown"
            android:textSize="16sp"/>

        <TextView
            android:id="@+id/tvNimMhs"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Jam"
            android:textColor="@color/text_brown"
            android:textSize="13sp"/>
    </LinearLayout>

    <androidx.cardview.widget.CardView
        android:id="@+id/cardStatus"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:cardCornerRadius="12dp"
        app:cardBackgroundColor="@color/status_hadir_bg"
        app:cardElevation="0dp">

        <TextView
            android:id="@+id/tvStatusRekap"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Hadir"
            android:textStyle="bold"
            android:textSize="12sp"
            android:textColor="@color/status_hadir_text"
            android:paddingHorizontal="12dp"
            android:paddingVertical="4dp"/>
    </androidx.cardview.widget.CardView>
</LinearLayout>